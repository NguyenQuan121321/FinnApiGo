info:
  name: totp-verify
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/mfa/totp/verify"
  body:
    type: json
    data: '{ "code": "748678" }'
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
