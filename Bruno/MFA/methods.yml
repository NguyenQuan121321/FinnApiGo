info:
  name: methods
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/mfa/methods"
  body: none
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
