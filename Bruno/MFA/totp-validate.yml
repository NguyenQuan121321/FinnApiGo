info:
  name: totp-validate
  type: http
  seq: 5

http:
  method: POST
  url: {{baseUrl}}/api/v1/auth/mfa/totp/validate
  body:
    type: json
    data: |-
      {
        "code": ""
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
