info:
  name: totp-enable
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/mfa/totp/enable"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
