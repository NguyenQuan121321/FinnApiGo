info:
  name: metrics
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/metrics"
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
