info:
  name: tenant-sessions
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/sessions"
  body:
    type: none
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

headers:
  - name: X-Tenant-ID
    value: "{{tenantId}}"
    enabled: true

settings:
  encodeUrl: true
