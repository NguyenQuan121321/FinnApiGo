info:
  name: audit-export
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/admin/audit-log/export?format=csv"
  body:
    type: none
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

headers:
  - name: X-Tenant-ID
    value: "{{tenantId}}"
    enabled: true

settings:
  encodeUrl: true
