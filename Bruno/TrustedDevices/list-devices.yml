info:
  name: list-devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/trusted-devices"
  body:
    type: none
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
