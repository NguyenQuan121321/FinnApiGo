info:
  name: revoke-device
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/auth/trusted-devices/{{deviceId}}"
  body:
    type: none
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
