info:
  name: passkey-list
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/mfa/passkeys"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
