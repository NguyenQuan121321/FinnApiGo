info:
  name: session-revoke
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/auth/sessions/{{sessionId}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
