info:
  name: change-email-confirm
  type: http
  seq: 19

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/change-email/confirm"
  body:
    type: json
    data: |-
      {
        "token": "staged-change-email-token"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
