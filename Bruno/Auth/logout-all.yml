info:
  name: logout-all
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/logout-all"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
