info:
  name: refresh-token
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/refresh-token
  body:
    type: json
    data: '{"refreshToken": "{{refreshToken}}"}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
