info:
  name: resend-verification
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/resend-verification
  body:
    type: json
    data: '{"email": "test1@example.com"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
