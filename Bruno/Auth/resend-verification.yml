info:
  name: resend-verification
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/resend-verification"
  body:
    type: json
    data: '{"email": "itzthecloud291@gmail.com"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
