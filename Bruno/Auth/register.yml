info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "finn",
        "email": "noclone0001@gmail.com",
        "password": "Password123",
        "fullName": "admin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
