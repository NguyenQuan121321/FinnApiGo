info:
  name: sessions
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/sessions"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
