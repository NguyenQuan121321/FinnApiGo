info:
  name: audit-log
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/me/audit-log?page=1&limit=20"
  body: none
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
