info:
  name: register-disposable-email
  type: http
  seq: 6

http:
  method: POST
  url: {{baseUrl}}/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "bot1",
        "email": "test@mailinator.com",
        "password": "Password1",
        "fullName": "Bot"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
