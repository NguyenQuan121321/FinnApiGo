info:
  name: register-honeypot
  type: http
  seq: 5

http:
  method: POST
  url: {{baseUrl}}/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "botuser",
        "email": "botuser@example.com",
        "password": "Password1",
        "fullName": "Bot User",
        "website": "http://spam.example.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
