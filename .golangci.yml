version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck    # unchecked errors
    - govet       # suspicious constructs
    - staticcheck # bugs, performance, style — subsumes gosimple + stylecheck in v2
    - ineffassign # useless assignments
    - unused      # unused code
    - gosec       # security scanner
    - gocritic    # opinionated quality checks
    - exhaustive  # enum switch completeness
    - nilerr      # returning nil when err != nil
    - errorlint   # proper error wrapping (errors.Is/As over == / type asserts)
    - depguard    # import restrictions
  settings:
    # Honor #nosec annotations — the SAME suppression mechanism the standalone
    # gosec CI gate (security job) uses: one annotation satisfies both tools,
    # and it lives only where the protocol genuinely requires the construct.
    gosec:
      nosec: true
    depguard:
      rules:
        services:
          files:
            - "**/internal/services/**/*.go"
          deny:
            - pkg: "github.com/gin-gonic/gin"
              desc: "Services must not depend on HTTP framework"
